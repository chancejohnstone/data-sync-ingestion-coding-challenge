{
  "name": "ingestion",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:integration": "vitest run --testPathPattern=integration",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "axios": "^1",
    "pg": "^8"
  },
  "devDependencies": {
    "@types/node": "*",
    "@types/pg": "*",
    "tsx": "*",
    "typescript": "^5",
    "vitest": "^2"
  }
}
